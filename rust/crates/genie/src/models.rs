//! Model lifecycle: HF download/verify (hf-hub), model-variant selection,
//! --verify-models, --uninstall. TODO M5.
use anyhow::Result;

pub fn verify() -> Result<()> {
    println!("genie (rust): --verify-models not yet implemented (RUST_PLAN.md M5).");
    Ok(())
}

pub fn uninstall() -> Result<()> {
    println!("genie (rust): --uninstall not yet implemented (RUST_PLAN.md M5).");
    Ok(())
}
